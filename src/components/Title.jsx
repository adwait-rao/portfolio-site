const Title = ({ text }) => {
  return (
    <div className="font-display flex-col py-8 text-center">
      <p className="inline-block font-bold text-colwhite text-4xl">{text}</p>
    </div>
  );
};

export default Title;
